import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.spps.cartilla',
  appName: 'cartilla-spps',
  webDir: 'dist/sakai',
  bundledWebRuntime: false
};

export default config;
